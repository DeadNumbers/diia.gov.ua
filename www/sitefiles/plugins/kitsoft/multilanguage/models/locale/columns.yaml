# ===================================
#  Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: yes
        invisible: true

    name:
        label: kitsoft.multilanguage::lang.locales.fields.name
        searchable: yes

    code:
        label: kitsoft.multilanguage::lang.locales.fields.code
        searchable: yes

    is_default:
        label: kitsoft.multilanguage::lang.locales.fields.is_default
        type: switch

    is_enabled:
        label: kitsoft.multilanguage::lang.locales.fields.is_enabled
        type: switch
        
